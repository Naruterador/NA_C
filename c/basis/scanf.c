#include <stdio.h>
int main(void){
	
	int i;
	scanf("%d",&i); //symbol & means fetch the address of variable from RAM.
	scanf("m%d",&q); //m123 is the correct input.
	scanf("%d %d",&x,&y); //input mutiple words.
	scanf("%d,%d,%d",&a,&b,&c); //example: 1,2,3 is the correct input.
	printf("i = %d",i);
	printf("q = %d",q);










}